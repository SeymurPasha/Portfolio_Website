import React from 'react'

export default function Arrow(props) {
    return (
        <div class={props.showArrow ? "arrow show" : "show"}>
                <span></span>
                <span></span>
                <span></span>
  
</div>
    )
}
